{
  "name": "claude-ds",
  "version": "0.1.2",
  "type": "module",
  "bin": { "claude-ds": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "commander": "^12.0.0" },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.5.0",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0"
  }
}
