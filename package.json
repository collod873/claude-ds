{
  "name": "claude-ds",
  "version": "0.7.10",
  "type": "module",
  "bin": {
    "claude-ds": "dist/cli.js"
  },
  "files": [
    "dist/",
    "packs/next-react/files/",
    "packs/next-react/manifest.json",
    "packs/next-react/MIGRATIONS.md",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc -p . && chmod +x dist/cli.js",
    "prepublishOnly": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "picomatch": "^4.0.4",
    "typescript": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/picomatch": "^4.0.3",
    "tsx": "^4.7.0",
    "vitest": "^1.5.0"
  }
}
