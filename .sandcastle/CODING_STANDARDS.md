# Coding standards for claude-ds

Add project-specific standards here that the reviewer agent should enforce. Examples:

- Avoid `any` in TypeScript — prefer specific types or `unknown` with narrowing
- Prefer pure functions and avoid side effects in helpers
- Tests live alongside the code they test (`src/foo.ts` + `tests/unit/foo.test.ts`)
- No `console.log` in committed code — use the project logger
