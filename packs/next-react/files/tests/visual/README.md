# Visual Regression Tests

CI baseline snapshots live here. See `design-system-scaffold.md` for the
full snapshot convention (`.snapshot.png` per component, generated by
`post-write-design.sh`, compared in CI).

Do not hand-author files in this directory — snapshots are generated artifacts.
